/**
 * Hook de autenticação.
 * Utilize useAuthContext() do AuthContext para acessar o estado de autenticação.
 * Este arquivo existe apenas para compatibilidade com importações legadas.
 */
export { useAuthContext as useAuth } from '@/contexts/AuthContext';
