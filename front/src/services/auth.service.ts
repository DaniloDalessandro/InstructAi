/**
 * Serviço de autenticação.
 * A lógica real de autenticação está em:
 * - contexts/AuthContext.tsx (estado global)
 * - lib/api/auth.ts (chamadas à API)
 * - lib/api/authFetch.ts (fetch autenticado com renovação de token)
 */
export {};
